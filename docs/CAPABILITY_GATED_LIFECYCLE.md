# Capability-Gated Lifecycle Planning

SPI validates product and plugin capability requirements before operation creation.

On Umbrel, denied requirements such as `systemd`, `apt`, `firewall`, `host-disk-management`, or `host-reboot` block the lifecycle before execution.
