# SPI Package 010 — Verification & Health Gate Engine

Requires every successful lifecycle execution to pass a structured post-action verification gate before SPI records success.

## Install

```bash
chmod +x scripts/*.sh
./scripts/doctor.sh
./scripts/install.sh
./scripts/verify.sh
```
