# SPI Package 005 — Capability-Gated Lifecycle Planning

Package 005 connects the verified Package 004 adapter model directly to lifecycle planning and execution.

## Install

```bash
chmod +x scripts/*.sh
./scripts/doctor.sh
./scripts/install.sh
./scripts/verify.sh
```

Package 005 remains dry-run only and performs no live host changes.
